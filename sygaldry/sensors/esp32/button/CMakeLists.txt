set(lib sygaldry-sensors-esp32-button)
add_library(${lib} INTERFACE)
target_include_directories(${lib} INTERFACE .)
target_link_libraries(${lib} INTERFACE sygaldry-sensors-portable-button)
target_link_libraries(${lib} INTERFACE sygaldry-sensors-esp32-gpio)
target_link_libraries(sygaldry-sensors-esp32 INTERFACE ${lib})
